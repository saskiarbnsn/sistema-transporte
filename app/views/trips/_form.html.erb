<% if trip.errors.any? %>
  <div class="alert alert-danger mb-3">
    <ul class="mb-0 ps-3">
      <% trip.errors.full_messages.each do |m| %><li><%= m %></li><% end %>
    </ul>
  </div>
<% end %>

<div class="ui-form-grid">
  <div>
    <%= form.label :customer_id, "Cliente", class: "form-label" %>
    <%= form.collection_select :customer_id, Customer.order(:name), :id, :name,
        { prompt: "Seleccionar..." }, { class: "form-select" } %>
  </div>
  <div>
    <%= form.label :field_id, "Campo (origen)", class: "form-label" %>
    <%= form.collection_select :field_id, Field.order(:name), :id, :name,
        { prompt: "Seleccionar..." },
        { class: "form-select",
          data: { "trip-map-target": "fieldSelect", action: "change->trip-map#update" } } %>
  </div>
  <div>
    <%= form.label :destination_id, "Destino", class: "form-label" %>
    <%= form.collection_select :destination_id, Destination.order(:name), :id, :name,
        { prompt: "Seleccionar..." },
        { class: "form-select",
          data: { "trip-map-target": "destinationSelect", action: "change->trip-map#update" } } %>
  </div>
  <div>
    <%= form.label :driver_id, "Chofer", class: "form-label" %>
    <%= form.collection_select :driver_id, Driver.order(:name), :id, :name,
        { prompt: "Seleccionar..." }, { class: "form-select" } %>
  </div>
  <div>
    <%= form.label :truck_id, "Camión", class: "form-label" %>
    <%= form.collection_select :truck_id, Truck.order(:plate), :id, :plate,
        { prompt: "Seleccionar..." }, { class: "form-select" } %>
  </div>
  <div>
    <%= form.label :product, "Producto", class: "form-label" %>
    <%= form.text_field :product, class: "form-control", placeholder: "Ej: Soja, Maíz, Trigo..." %>
  </div>
  <div>
    <%= form.label :weight, "Peso", class: "form-label" %>
    <div class="ui-input-unit">
      <%= form.number_field :weight, class: "form-control", step: "any", min: 0,
            data: { "trip-calc-target": "weight",
                    action: "input->trip-calc#calculate" } %>
      <span class="ui-input-unit__label">Kg</span>
    </div>
  </div>
  <div>
    <%= form.label :tariff, "Tarifa ($/t)", class: "form-label" %>
    <div class="ui-input-unit">
      <%= form.number_field :tariff, class: "form-control", step: "any", min: 0,
            data: { "trip-calc-target": "tariff",
                    action: "input->trip-calc#calculate" } %>
      <span class="ui-input-unit__label">$/t</span>
    </div>
  </div>
  <div>
    <%= form.label :kilometres, "Kilómetros", class: "form-label" %>
    <div class="ui-input-unit">
      <%= form.number_field :kilometres, class: "form-control", step: "any", min: 0 %>
      <span class="ui-input-unit__label">km</span>
    </div>
  </div>
  <div>
    <%= form.label :date, "Fecha de inicio", class: "form-label" %>
    <%= form.date_field :date, class: "form-control",
          data: { action: "change->trip-dates#updateMin" } %>
  </div>
  <div>
    <%= form.label :date_end, "Fecha de finalización", class: "form-label" %>
    <%= form.date_field :date_end, class: "form-control",
          min: trip.date&.to_date&.iso8601,
          data: { "trip-dates-target": "dateEnd" } %>
  </div>
</div>
