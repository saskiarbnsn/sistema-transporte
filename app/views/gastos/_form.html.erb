<%= form_with(model: gasto, data: { controller: "gasto-form" }) do |f| %>
  <% if gasto.errors.any? %>
    <div class="form-errors mb-3">
      <i class="bi bi-exclamation-circle-fill form-errors__icon"></i>
      <div class="form-errors__body">
        <p class="form-errors__title">
          <%= gasto.errors.count == 1 ? "1 error" : "#{gasto.errors.count} errores" %> impidieron guardar:
        </p>
        <ul class="form-errors__list">
          <% gasto.errors.full_messages.each do |m| %><li><%= m %></li><% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <%# — Propios —%>
  <div class="gasto-propios">
    <span class="gasto-propios__label">Propios</span>
    <label class="gasto-propios__opt">
      <%= f.radio_button :truck_disabled, "true" %>
      <span>Si</span>
    </label>
    <label class="gasto-propios__opt">
      <%= f.radio_button :truck_disabled, "false", checked: !gasto.truck_disabled? %>
      <span>No</span>
    </label>
  </div>

  <%# — Grid de campos —%>
  <div class="gasto-grid">
    <div class="gasto-field">
      <%= f.label :imputation_id, "Imputación", class: "gasto-label" %>
      <%= f.collection_select :imputation_id, imputations, :id, :imputation,
            { prompt: "Seleccionar" }, class: "form-select gasto-select" %>
    </div>
    <div class="gasto-field">
      <%= f.label :date, "Fecha", class: "gasto-label" %>
      <%= f.date_field :date, class: "form-control gasto-input" %>
    </div>
    <div class="gasto-field">
      <%= f.label :supplier, "Proveedor", class: "gasto-label" %>
      <%= f.text_field :supplier, class: "form-control gasto-input" %>
    </div>
    <div class="gasto-field">
      <%= f.label :description, "Concepto / Descripción", class: "gasto-label" %>
      <%= f.text_field :description, class: "form-control gasto-input" %>
    </div>
    <div class="gasto-field">
      <%= f.label :truck_id, "Seleccionar camión", class: "gasto-label" %>
      <%= f.collection_select :truck_id, Truck.order(:plate), :id, :plate,
            { include_blank: "Seleccionar" }, class: "form-select gasto-select" %>
    </div>
    <div class="gasto-field">
      <label class="gasto-label">Adelantos</label>
      <input class="form-control gasto-input gasto-input--disabled" disabled>
    </div>
  </div>

  <%# — Importe total —%>
  <div class="gasto-totals">
    <div class="gasto-totals__left">
      <p class="gasto-totals__title">Importe Total</p>
      <%= f.number_field :total, class: "gasto-total-input",
            step: "0.01", min: 0, placeholder: "$ 0,00",
            data: { "gasto-form-target": "total", action: "input->gasto-form#calculate" } %>
    </div>
    <div class="gasto-totals__breakdown">
      <div class="gasto-totals__col">
        <span class="gasto-totals__col-label">Neto</span>
        <span class="gasto-totals__col-value" data-gasto-form-target="netoDisplay">$ 0,00</span>
      </div>
      <div class="gasto-totals__sep"></div>
      <div class="gasto-totals__col">
        <span class="gasto-totals__col-label">IVA</span>
        <span class="gasto-totals__col-value" data-gasto-form-target="ivaDisplay">$ 0,00</span>
      </div>
      <div class="gasto-totals__sep"></div>
      <div class="gasto-totals__col">
        <span class="gasto-totals__col-label">Gravado</span>
        <span class="gasto-totals__col-value">$0,00</span>
      </div>
    </div>
  </div>

  <%# — Acciones —%>
  <div class="gasto-actions">
    <%= f.submit gasto.new_record? ? "Confirmar" : "Actualizar", class: "btn-pill-primary" %>
    <%= link_to "Cancelar", gastos_path, class: "btn-pill-secondary" %>
  </div>
<% end %>
